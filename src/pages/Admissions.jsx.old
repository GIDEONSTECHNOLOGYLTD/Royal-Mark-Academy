import { useState } from 'react';
import { FaGraduationCap, FaClipboardList, FaFileAlt, FaCheckCircle, FaCalendarAlt, FaIdCard, FaUserGraduate, FaChalkboardTeacher, FaBuilding, FaStar, FaUniversity, FaUserFriends, FaCalendarCheck, FaDownload, FaArrowRight } from 'react-icons/fa';
import { motion, AnimatePresence } from 'framer-motion';

function Admissions() {
  const [activeTab, setActiveTab] = useState('requirements');
  
  // Animation definitions
  
  const tabVariants = {
    inactive: { scale: 1 },
    active: { 
      scale: 1.05, 
      boxShadow: '0 10px 25px -5px rgba(59, 130, 246, 0.5)' 
    }
  };
  
  const processSteps = [
    { id: 1, title: 'Submit Application', icon: FaClipboardList, desc: 'Complete and submit the online application form along with all required documents.' },
    { id: 2, title: 'Entrance Assessment', icon: FaChalkboardTeacher, desc: 'Students take part in our comprehensive academic and aptitude assessment.' },
    { id: 3, title: 'Family Interview', icon: FaUserGraduate, desc: 'Meet with our admissions team to discuss educational goals and school values.' },
    { id: 4, title: 'Decision & Enrollment', icon: FaCheckCircle, desc: 'Receive an admission decision and complete the enrollment process if accepted.' },
  ];
  
  return (
    <div className="bg-gray-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        {/* Premium Hero Banner with Advanced Animation Effects */}
        <div className="relative mb-20 overflow-hidden">
          <motion.div 
            className="bg-gradient-to-r from-blue-900 via-indigo-800 to-blue-800 rounded-3xl overflow-hidden"
            initial={{ opacity: 0, height: '60vh' }}
            animate={{ opacity: 1, height: '70vh' }}
            transition={{ duration: 1.2, ease: [0.22, 1, 0.36, 1] }}
          >
            {/* Decorative Elements */}
            <div className="absolute inset-0 overflow-hidden">
              <div className="absolute top-0 left-0 w-96 h-96 bg-blue-400 rounded-full mix-blend-overlay filter blur-3xl opacity-20 animate-blob"></div>
              <div className="absolute top-0 right-0 w-80 h-80 bg-indigo-300 rounded-full mix-blend-overlay filter blur-3xl opacity-20 animate-blob animation-delay-2000"></div>
              <div className="absolute -bottom-32 left-20 w-96 h-96 bg-yellow-200 rounded-full mix-blend-overlay filter blur-3xl opacity-15 animate-blob animation-delay-4000"></div>
              
              {/* Background Pattern */}
              <div className="absolute inset-0 bg-[url('/images/pattern-grid.svg')] opacity-5"></div>
              
              {/* School Building Silhouette - Optional */}
              <div className="absolute bottom-0 right-0 w-full h-32 bg-black opacity-20 blur-sm" 
                   style={{ clipPath: 'polygon(100% 0%, 100% 100%, 0% 100%, 20% 80%, 30% 90%, 40% 70%, 50% 85%, 60% 75%, 70% 90%, 80% 65%, 90% 80%)' }}></div>
            </div>

            {/* Content */}
            <div className="relative h-full flex flex-col justify-center items-start text-left px-8 sm:px-12 lg:px-16 max-w-6xl mx-auto">
              <motion.span 
                className="text-yellow-300 font-medium text-lg mb-3 inline-block tracking-wider"
                initial={{ opacity: 0, y: -20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.6, delay: 0.2 }}
              >
                ADMISSIONS OPEN FOR 2024-2025
              </motion.span>
              
              <motion.h1 
                className="text-5xl md:text-6xl lg:text-7xl font-bold text-white mb-6 leading-tight"
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, delay: 0.3 }}
              >
                <span className="block">Begin Your Journey at</span>
                <span className="text-transparent bg-clip-text bg-gradient-to-r from-yellow-200 to-yellow-400">Royal Mark Academy</span>
              </motion.h1>
              
              <motion.p 
                className="text-xl text-blue-100 max-w-2xl mb-10"
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, delay: 0.4 }}
              >
                Discover our world-class education at Number 1 Elepa Road, Odeda, Abeokuta. 
                Since 2014, we've been nurturing the next generation of leaders through academic excellence and character development.
              </motion.p>
              
              <motion.div 
                className="flex flex-wrap gap-6"
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, delay: 0.5 }}
              >
                <motion.a 
                  href="#requirements"
                  className="px-10 py-4 bg-gradient-to-r from-white to-blue-50 text-blue-900 rounded-xl font-semibold shadow-xl hover:shadow-2xl flex items-center justify-center focus:ring-4 focus:ring-blue-100 focus:outline-none group"
                  whileHover={{ scale: 1.03, y: -4, boxShadow: '0 25px 25px -5px rgba(59, 130, 246, 0.25)' }}
                  whileTap={{ scale: 0.98 }}
                  aria-label="View admission requirements"
                >
                  <FaClipboardList className="mr-3 text-lg text-blue-700 group-hover:scale-110 transition-transform" />
                  <span>View Requirements</span>
                </motion.a>
                
                <motion.a
                  href="#process" 
                  className="px-10 py-4 bg-gradient-to-r from-yellow-500 to-yellow-400 text-blue-900 rounded-xl font-semibold shadow-xl hover:shadow-2xl flex items-center justify-center focus:ring-4 focus:ring-yellow-200 focus:outline-none group"
                  whileHover={{ scale: 1.03, y: -4, boxShadow: '0 25px 25px -5px rgba(234, 179, 8, 0.25)' }}
                  whileTap={{ scale: 0.98 }}
                  aria-label="Start your application"
                >
                  <FaGraduationCap className="mr-3 text-lg text-blue-800 group-hover:rotate-12 transition-transform" />
                  <span>Apply Now</span>
                </motion.a>
              </motion.div>
              
              {/* Admissions Contact */}
              <motion.div 
                className="absolute bottom-4 right-8 flex items-center space-x-2 text-sm text-blue-100 bg-blue-800/30 backdrop-blur-sm rounded-full px-4 py-2"
                initial={{ opacity: 0, x: 20 }}
                animate={{ opacity: 1, x: 0 }}
                transition={{ duration: 0.6, delay: 1 }}
              >
                <span>Need help?</span>
                <span className="font-medium">+234-123-456-7890</span>
              </motion.div>
            </div>
          </motion.div>
        </div>
        
        {/* Stats Section with 3D Cards and Animations */}
        <div className="container mx-auto px-4 py-8">
          <motion.div 
            className="relative bg-white rounded-2xl shadow-2xl p-10 mb-16 border-t-4 border-blue-600 overflow-hidden"
            initial={{ opacity: 0, y: 50 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: true, margin: "-100px" }}
          >
            <div className="absolute top-0 right-0 w-64 h-64 bg-blue-50 rounded-full filter blur-3xl opacity-30 -mr-20 -mt-20"></div>
            <div className="absolute bottom-0 left-0 w-64 h-64 bg-indigo-50 rounded-full filter blur-3xl opacity-30 -ml-20 -mb-20"></div>
            
            <motion.h2 
              className="text-3xl font-bold text-center text-gray-800 mb-10 relative z-10"
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              transition={{ delay: 0.2 }}
              viewport={{ once: true }}
            >
              Royal Mark Academy at a Glance
            </motion.h2>
            
            <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-8">
              {[
                { icon: FaUserFriends, stat: '94%', desc: 'Student Retention Rate', color: 'from-blue-400 to-blue-600' },
                { icon: FaUniversity, stat: '100%', desc: 'College Acceptance Rate', color: 'from-indigo-400 to-indigo-600' },
                { icon: FaStar, stat: '8:1', desc: 'Student-Teacher Ratio', color: 'from-blue-500 to-indigo-600' },
                { icon: FaCalendarCheck, stat: '98%', desc: 'Attendance Rate', color: 'from-blue-400 to-blue-600' },
              ].map((item, i) => (
                <motion.div 
                  key={i}
                  className="bg-gradient-to-br from-white to-blue-50 rounded-xl p-6 shadow-lg relative overflow-hidden group"
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  transition={{ delay: 0.1 * i, type: "spring", stiffness: 100 }}
                  viewport={{ once: true }}
                  whileHover={{ y: -5, boxShadow: '0 15px 30px -5px rgba(59, 130, 246, 0.4)' }}
                >
                  <div className={`absolute top-0 right-0 w-24 h-24 bg-gradient-to-br ${item.color} rounded-full opacity-10 -mr-10 -mt-10 group-hover:scale-150 transition-transform duration-500 ease-out`}></div>
                  <div className={`absolute bottom-0 left-0 w-20 h-20 bg-gradient-to-tr ${item.color} rounded-full opacity-5 -ml-10 -mb-10 group-hover:scale-150 transition-transform duration-700 ease-out`}></div>
                  
                  <div className="flex items-center justify-start mb-4">
                    <motion.div 
                      className={`p-3 rounded-lg bg-gradient-to-br ${item.color} text-white mr-4 shadow-md`}
                      whileHover={{ rotate: [0, -10, 10, -5, 0], scale: 1.1 }}
                      transition={{ duration: 0.5 }}
                    >
                      <item.icon size={24} />
                    </motion.div>
                  </div>
                  
                  <motion.h3 
                    className="text-4xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-blue-800 to-indigo-700"
                    initial={{ scale: 0.9 }}
                    whileInView={{ scale: 1 }}
                    transition={{ type: "spring", stiffness: 200 }}
                  >
                    {item.stat}
                  </motion.h3>
                  <p className="text-gray-600 font-medium">{item.desc}</p>
                </motion.div>
              ))}
            </div>
          </motion.div>
        </div>
        
        {/* Main Content Card with Enhanced 3D Effects */}
        <div className="my-12">
          <motion.div 
            className="bg-white rounded-xl shadow-2xl overflow-hidden transform-gpu"
            initial={{ opacity: 0, y: 50 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            whileHover={{ boxShadow: '0 25px 50px -12px rgba(59, 130, 246, 0.25)' }}
          >
            <div className="bg-gradient-to-r from-blue-600 via-indigo-600 to-blue-700 h-3 animate-gradient-x"></div>
            <div className="relative z-10 p-8 md:p-10">
              {/* Enhanced Tab Navigation with Improved Accessibility */}
              <div className="flex flex-wrap md:flex-nowrap justify-center gap-4 sm:gap-6 mb-12">
                {['requirements', 'process', 'forms'].map((tab) => {
                  const isActive = activeTab === tab;
                  const TabIcon = tab === 'requirements' 
                    ? FaGraduationCap 
                    : tab === 'process' 
                      ? FaClipboardList 
                      : FaFileAlt;
                  
                  const tabName = tab === 'requirements' 
                    ? 'Requirements' 
                    : tab === 'process' 
                      ? 'Application Process' 
                      : 'Forms & Documents';
                  
                  return (
                    <motion.button
                      key={tab}
                      onClick={() => setActiveTab(tab)}
                      className={`flex items-center px-4 sm:px-6 py-4 rounded-xl transition-all ${
                        isActive 
                          ? 'bg-gradient-to-r from-blue-600 to-indigo-600 text-white ring-4 ring-blue-100' 
                          : 'bg-white hover:bg-blue-50 text-gray-800 shadow-md hover:shadow-lg border border-gray-100'
                      }`}
                      variants={tabVariants}
                      animate={isActive ? 'active' : 'inactive'}
                      whileHover={{ scale: isActive ? 1.02 : 1.05 }}
                      whileTap={{ scale: 0.98 }}
                      aria-pressed={isActive}
                      aria-label={`Show ${tabName} tab`}
                      role="tab"
                      aria-selected={isActive}
                    >
                      <motion.div 
                        className={`${isActive ? 'text-white' : 'text-blue-600'} mr-2 sm:mr-3 text-xl flex items-center justify-center`}
                        animate={{ rotate: isActive ? [0, 10, 0] : 0 }}
                        transition={{ duration: 0.5, ease: "easeInOut" }}
                      >
                        <TabIcon size={20} aria-hidden="true" />
                      </motion.div>
                      <span className="font-semibold tracking-wide text-sm sm:text-base">
                        {tabName}
                      </span>
                    </motion.button>
                  );
                })}
              </div>
              {/* Tab Content with Enhanced Visual Appeal */}
              <div className="bg-white rounded-2xl p-6 md:p-10">
                <AnimatePresence mode="wait">
                  <motion.div
                    key={activeTab}
                    initial={{ opacity: 0, y: 20, scale: 0.98 }}
                    animate={{ opacity: 1, y: 0, scale: 1 }}
                    exit={{ opacity: 0, y: -20, scale: 0.98 }}
                    transition={{ duration: 0.5, type: "spring", stiffness: 100 }}
                    className="relative overflow-hidden"
                  >
                  {activeTab === 'requirements' && (
                    <div>
                      <h2 className="text-3xl font-bold mb-6 text-blue-800">Admission Requirements</h2>
                      
                      <div className="grid md:grid-cols-2 gap-6 mb-8">
                        <div className="bg-blue-50 p-6 rounded-lg border-l-4 border-blue-600">
                          <h3 className="text-xl font-semibold mb-3 text-blue-700">Academic Standards</h3>
                          <ul className="list-disc pl-5 space-y-2">
                            <li>Strong academic record from previous institution</li>
                            <li>Satisfactory performance on entrance assessment</li>
                            <li>Recommendation letters from previous teachers</li>
                          </ul>
                        </div>
                        
                        <div className="bg-indigo-50 p-6 rounded-lg border-l-4 border-indigo-600">
                          <h3 className="text-xl font-semibold mb-3 text-indigo-700">Character & Values</h3>
                          <ul className="list-disc pl-5 space-y-2">
                            <li>Demonstrated integrity and positive behavior</li>
                            <li>Alignment with Royal Mark Academy's core values</li>
                            <li>Willingness to participate in school activities</li>
                          </ul>
                        </div>
                      </div>
                      
                      <div className="bg-gradient-to-r from-blue-50 to-indigo-50 p-6 rounded-lg">
                        <h3 className="text-xl font-semibold mb-3 text-gray-800">Required Documents</h3>
                        <div className="grid md:grid-cols-2 gap-4">
                          <div className="flex items-start">
                            <FaIdCard className="text-blue-600 mt-1 mr-3 text-lg" />
                            <span>Birth certificate or passport</span>
                          </div>
                          <div className="flex items-start">
                            <FaCheckCircle className="text-blue-600 mt-1 mr-3 text-lg" />
                            <span>Academic records (last 2 years)</span>
                          </div>
                          <div className="flex items-start">
                            <FaFileAlt className="text-blue-600 mt-1 mr-3 text-lg" />
                            <span>Completed application form</span>
                          </div>
                          <div className="flex items-start">
                            <FaCalendarAlt className="text-blue-600 mt-1 mr-3 text-lg" />
                            <span>Immunization records</span>
                          </div>
                        </div>
                      </div>
                    </div>
                  )}
                  
                  {activeTab === 'process' && (
                    <div>
                      <motion.h3 
                        className="text-2xl font-bold text-gray-800 mb-6"
                        initial={{ opacity: 0, y: -10 }}
                        animate={{ opacity: 1, y: 0 }}
                        transition={{ duration: 0.4 }}
                      >
                        Our Application Process
                      </motion.h3>
                      
                      <motion.p 
                        className="text-gray-600 mb-10"
                        initial={{ opacity: 0 }}
                        animate={{ opacity: 1 }}
                        transition={{ duration: 0.4, delay: 0.2 }}
                      >
                        Our admissions process is designed to be thorough yet straightforward, helping us find students who will thrive at Royal Mark Academy while giving families all the information they need to make the right educational choice.
                      </motion.p>
                      
                      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                        {processSteps.map((step, index) => (
                          <motion.div 
                            key={step.id} 
                            className="bg-gradient-to-b from-white to-blue-50 p-6 rounded-xl border border-blue-100 shadow-lg hover:shadow-xl transition-all relative overflow-hidden group"
                            initial={{ opacity: 0, y: 30 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ duration: 0.5, delay: 0.2 + index * 0.1 }}
                            whileHover={{ y: -5, boxShadow: '0 20px 30px -10px rgba(59, 130, 246, 0.3)' }}
                          >
                            <div className="absolute top-0 right-0 w-20 h-20 bg-blue-100 rounded-full opacity-50 -mr-10 -mt-10 z-0"></div>
                            <motion.div 
                              className="inline-flex justify-center items-center w-16 h-16 rounded-2xl bg-gradient-to-r from-blue-500 to-indigo-600 text-white text-2xl mb-4 shadow-lg relative z-10"
                              whileHover={{ rotate: [0, -10, 10, -10, 0], scale: 1.05 }}
                              transition={{ duration: 0.5 }}
                            >
                              <step.icon size={24} />
                            </motion.div>
                            <h4 className="text-xl font-bold text-blue-800 mb-2 relative z-10">Step {step.id}: {step.title}</h4>
                            <p className="text-gray-600 relative z-10">{step.desc}</p>
                            <div className="absolute bottom-2 right-2 w-6 h-6 rounded-full bg-blue-200 opacity-0 group-hover:opacity-50 transition-opacity"></div>
                          </motion.div>
                        ))}
                      </div>
                      
                      <motion.div 
                        className="mt-12 p-6 bg-blue-50 rounded-lg border border-blue-200"
                        initial={{ opacity: 0, y: 20 }}
                        animate={{ opacity: 1, y: 0 }}
                        transition={{ duration: 0.4, delay: 0.6 }}
                      >
                        <h3 className="text-xl font-semibold mb-4 text-blue-800">Key Dates</h3>
                        <div className="grid md:grid-cols-3 gap-4 text-center">
                          <motion.div 
                            className="p-4 bg-white rounded-lg shadow"
                            whileHover={{ y: -2, boxShadow: '0 10px 15px -3px rgba(59, 130, 246, 0.2)' }}
                            transition={{ type: "spring", stiffness: 300 }}
                          >
                            <h4 className="font-medium text-blue-700">Early Admission</h4>
                            <p className="text-lg font-bold">November 15, 2024</p>
                          </motion.div>
                          <motion.div 
                            className="p-4 bg-white rounded-lg shadow"
                            whileHover={{ y: -2, boxShadow: '0 10px 15px -3px rgba(59, 130, 246, 0.2)' }}
                            transition={{ type: "spring", stiffness: 300 }}
                          >
                            <h4 className="font-medium text-blue-700">Regular Admission</h4>
                            <p className="text-lg font-bold">February 1, 2025</p>
                          </motion.div>
                          <motion.div 
                            className="p-4 bg-white rounded-lg shadow"
                            whileHover={{ y: -2, boxShadow: '0 10px 15px -3px rgba(59, 130, 246, 0.2)' }}
                            transition={{ type: "spring", stiffness: 300 }}
                          >
                            <h4 className="font-medium text-blue-700">Late Admission</h4>
                            <p className="text-lg font-bold">May 15, 2025</p>
                          </motion.div>
                        </div>
                      </motion.div>
                    </div>
                  )}
                  
                  {activeTab === 'forms' && (
                    <div>
                      <motion.h2 
                        className="text-3xl font-bold mb-4 text-transparent bg-clip-text bg-gradient-to-r from-blue-800 via-indigo-700 to-blue-800"
                        initial={{ opacity: 0, y: -10 }}
                        animate={{ opacity: 1, y: 0 }}
                        transition={{ duration: 0.5 }}
                      >
                        Forms & Documents
                      </motion.h2>
                      
                      <p className="text-gray-700 mb-6 max-w-3xl">
                        Please download and complete the following forms as part of your application to Royal Mark Academy. If you need assistance, contact our admissions office at <span className="text-blue-700 font-medium">admissions@royalmarkacademy.edu.ng</span> or visit us at Number 1 Elepa Road, Odeda, Abeokuta.
                      </p>
                      
                      <div className="grid md:grid-cols-2 gap-6 mb-10">
                        {[
                          { title: 'Student Application Form', desc: 'Main application for student enrollment', btnText: 'Download PDF', icon: FaIdCard },
                          { title: 'Health & Medical Form', desc: 'Required medical information and emergency contacts', btnText: 'Download PDF', icon: FaFileAlt },
                          { title: 'Financial Aid Application', desc: 'For families seeking tuition assistance', btnText: 'Download PDF', icon: FaUniversity },
                          { title: 'Transfer Student Form', desc: 'Additional form for students transferring from another school', btnText: 'Download PDF', icon: FaUserGraduate },
                        ].map((form, idx) => (
                          <motion.div 
                            key={idx}
                            className="bg-gradient-to-br from-white to-blue-50 p-6 rounded-lg shadow-md border border-blue-50 relative overflow-hidden group"
                            initial={{ opacity: 0, y: 20 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ duration: 0.4, delay: idx * 0.1 }}
                            whileHover={{ 
                              scale: 1.02,
                              boxShadow: '0 10px 25px -5px rgba(59, 130, 246, 0.4)'
                            }}
                          >
                            <div className="absolute top-0 right-0 w-20 h-20 bg-blue-50 rounded-full -mr-10 -mt-10 group-hover:scale-150 transition-transform duration-500"></div>
                            
                            <div className="flex items-start mb-3">
                              <div className="p-3 bg-gradient-to-r from-blue-600 to-indigo-600 rounded-lg text-white mr-4 shadow-md">
                                <form.icon size={20} aria-hidden="true" />
                              </div>
                              <h3 className="text-xl font-semibold text-blue-800 pt-1">{form.title}</h3>
                            </div>
                            
                            <p className="text-gray-600 mb-5 pl-14">{form.desc}</p>
                            
                            <motion.button 
                              className="px-4 py-2 bg-gradient-to-r from-blue-600 to-indigo-600 text-white rounded-lg hover:from-blue-700 hover:to-indigo-700 transition-all flex items-center ml-14 focus:ring-4 focus:ring-blue-300 focus:outline-none"
                              whileHover={{ scale: 1.03 }}
                              whileTap={{ scale: 0.98 }}
                              aria-label={`Download ${form.title}`}
                            >
                              <FaDownload className="mr-2" aria-hidden="true" />
                              {form.btnText}
                            </motion.button>
                          </motion.div>
                        ))}
                      </div>
                      
                      <motion.div 
                        className="mt-8 p-6 bg-gradient-to-br from-indigo-50 to-blue-50 rounded-lg border border-indigo-100 shadow-md relative overflow-hidden"
                        initial={{ opacity: 0, y: 20 }}
                        animate={{ opacity: 1, y: 0 }}
                        transition={{ duration: 0.5, delay: 0.4 }}
                      >
                        <div className="absolute top-0 left-0 w-40 h-40 bg-indigo-100 rounded-full -mt-20 -ml-20 opacity-50"></div>
                        <div className="absolute bottom-0 right-0 w-40 h-40 bg-blue-100 rounded-full -mb-20 -mr-20 opacity-50"></div>
                        
                        <div className="relative z-10">
                          <div className="flex items-center mb-4">
                            <FaCheckCircle className="text-indigo-600 mr-3 text-xl" aria-hidden="true" />
                            <h3 className="text-xl font-semibold text-indigo-800">Submission Instructions</h3>
                          </div>
                          
                          <p className="mb-5 text-gray-700">Please submit all completed forms and supporting documents in one of the following ways:</p>
                          
                          <div className="space-y-4">
                            {[
                              { method: 'Upload through our secure online portal', note: '(recommended)', icon: FaUniversity },
                              { method: 'Email to', email: 'admissions@royalmarkacademy.edu.ng', icon: FaFileAlt },
                              { method: 'In-person at our Administrative Office', note: 'during business hours (8:00 AM - 4:00 PM)', icon: FaBuilding }
                            ].map((option, idx) => (
                              <motion.div 
                                key={idx}
                                className="flex items-center bg-white p-3 rounded-lg shadow-sm"
                                initial={{ opacity: 0, x: -10 }}
                                animate={{ opacity: 1, x: 0 }}
                                transition={{ delay: 0.6 + (idx * 0.1) }}
                                whileHover={{ x: 5, backgroundColor: 'rgba(255, 255, 255, 0.9)' }}
                              >
                                <div className="bg-indigo-100 text-indigo-700 p-2 rounded-lg mr-3">
                                  <option.icon size={18} aria-hidden="true" />
                                </div>
                                <div>
                                  <span className="font-medium">{option.method} </span>
                                  {option.email && <span className="text-indigo-600 font-medium">{option.email}</span>}
                                  {option.note && <span className="text-gray-500 text-sm"> {option.note}</span>}
                                </div>
                              </motion.div>
                            ))}
                          </div>
                        </div>
                      </motion.div>
                    </div>
                  )}
                  </motion.div>
                </AnimatePresence>
              </div>
            </div>
          </motion.div>
        </div>
        
        {/* Premium Testimonials with Advanced UI */}
        <div className="my-20 relative">
          {/* Decorative background elements */}
          <div className="absolute inset-0 overflow-hidden z-0">
            <div className="absolute top-20 left-10 w-64 h-64 bg-blue-100 rounded-full filter blur-3xl opacity-30"></div>
            <div className="absolute bottom-20 right-10 w-72 h-72 bg-indigo-100 rounded-full filter blur-3xl opacity-30"></div>
          </div>
          
          <div className="relative z-10">
            <motion.h2 
              className="text-3xl md:text-4xl font-bold text-center mb-4 text-transparent bg-clip-text bg-gradient-to-r from-blue-800 via-indigo-700 to-blue-900"
              initial={{ opacity: 0, y: -10 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5 }}
              viewport={{ once: true }}
            >
              Voices from Our Community
            </motion.h2>
            
            <motion.div 
              className="h-1.5 w-32 bg-gradient-to-r from-yellow-400 to-yellow-300 rounded-full mx-auto mb-6"
              initial={{ width: 0, opacity: 0 }}
              whileInView={{ width: 128, opacity: 1 }}
              transition={{ duration: 0.8 }}
              viewport={{ once: true }}
            />
            
            <motion.p 
              className="text-center text-gray-600 mb-16 max-w-3xl mx-auto text-lg"
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              transition={{ duration: 0.5, delay: 0.3 }}
              viewport={{ once: true }}
            >
              Since our founding in 2014 at Number 1 Elepa Road, Odeda, Abeokuta, we've nurtured generations of
              exceptional students. Hear what our community has to say about the Royal Mark experience.
            </motion.p>
            
            <div className="grid md:grid-cols-3 gap-8">
            {[
              { name: 'Chioma A.', grade: 'Senior Student', quote: 'Royal Mark Academy provided me with the perfect environment to discover and develop my talents. The teachers are incredibly supportive and encouraging.' },
              { name: 'Adebayo T.', grade: 'Alumni, Class of 2023', quote: 'My time at Royal Mark prepared me exceptionally well for university. The rigorous academic programs and leadership opportunities gave me a competitive edge.' },
              { name: 'Fatima M.', grade: 'Junior Student', quote: 'I love the diverse and inclusive community at Royal Mark. There are so many extracurricular activities to explore and friends to make.' },
            ].map((testimonial, i) => (
              <motion.div
                key={i}
                className="bg-gradient-to-br from-blue-50 to-indigo-50 p-6 rounded-xl shadow-lg relative overflow-hidden group"
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.2 * i, duration: 0.5 }}
                viewport={{ once: true }}
                whileHover={{ y: -5, boxShadow: '0 20px 25px -5px rgba(59, 130, 246, 0.25)' }}
              >
                <div className="absolute top-0 right-0 w-40 h-40 bg-blue-100 rounded-full -mt-20 -mr-20 group-hover:scale-110 transition-transform duration-500 opacity-30"></div>
                
                <motion.div 
                  className="absolute text-indigo-300 h-16 w-16 -top-6 -left-6"
                  whileHover={{ scale: 1.1, rotate: [0, -10, 10, -10, 0] }}
                  transition={{ duration: 0.5 }}
                >
                  <svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M14.017 18L14.017 10.609C14.017 4.905 17.748 1.039 23 0L23.995 2.151C21.563 3.068 20 5.789 20 8H24V18H14.017ZM0 18V10.609C0 4.905 3.748 1.039 9 0L9.996 2.151C7.563 3.068 6 5.789 6 8H9.983L9.983 18L0 18Z" />
                  </svg>
                </motion.div>
                
                <p className="text-gray-700 mb-4 relative z-10 italic font-medium">"{testimonial.quote}"</p>
                
                <div className="relative z-10 flex items-center mt-4 pt-4 border-t border-blue-100">
                  <div className="bg-gradient-to-r from-blue-600 to-indigo-600 w-10 h-10 rounded-full flex items-center justify-center text-white font-bold mr-3">
                    {testimonial.name.charAt(0)}
                  </div>
                  <div>
                    <p className="font-bold text-blue-900">{testimonial.name}</p>
                    <p className="text-sm text-blue-600">{testimonial.grade}</p>
                  </div>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
        
        {/* Enhanced CTA Banner with Parallax Effect */}
        <motion.div 
          className="relative overflow-hidden bg-cover bg-center rounded-xl my-16" 
          style={{ backgroundImage: "url('/images/school-building.jpg')" }}
          initial={{ height: '16rem' }}
          whileInView={{ height: '20rem' }}
          transition={{ duration: 0.5 }}
          viewport={{ once: true }}
        >
          <div className="absolute inset-0 bg-gradient-to-r from-blue-900/90 to-indigo-900/80"></div>
          
          {/* Decorative Elements */}
          <div className="absolute inset-0 overflow-hidden">
            <div className="absolute -top-10 -right-10 w-40 h-40 bg-blue-400/10 rounded-full"></div>
            <div className="absolute top-1/2 -left-10 w-32 h-32 bg-indigo-400/10 rounded-full"></div>
            <div className="absolute -bottom-10 right-1/4 w-24 h-24 bg-blue-300/10 rounded-full"></div>
          </div>
          
          <div className="relative h-full flex flex-col items-center justify-center text-white p-8">
            <motion.h2 
              className="text-3xl sm:text-4xl font-bold mb-4 text-center"
              initial={{ opacity: 0, y: -20 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.1 }}
              viewport={{ once: true }}
            >
              Ready to Join Royal Mark Academy?
            </motion.h2>
            
            <motion.p 
              className="text-lg mb-8 max-w-2xl text-center mx-auto text-blue-50"
              initial={{ opacity: 0, y: -10 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.3 }}
              viewport={{ once: true }}
            >
              We're excited to welcome your child to our community of learners and future leaders at Number 1 Elepa Road, Odeda, Abeokuta.
            </motion.p>
            
            <motion.div 
              className="flex flex-wrap gap-4 justify-center"
              initial={{ opacity: 0, y: 10 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.5 }}
              viewport={{ once: true }}
            >
              <motion.button 
                className="px-8 py-3 bg-gradient-to-r from-white to-blue-50 text-blue-800 rounded-lg font-medium shadow-lg hover:shadow-xl focus:ring-4 focus:ring-blue-100 focus:outline-none flex items-center group"
                whileHover={{ scale: 1.03, y: -2 }}
                whileTap={{ scale: 0.98 }}
                aria-label="Start application process"
              >
                <span>Apply Now</span>
                <FaArrowRight className="ml-2 group-hover:translate-x-1 transition-transform" aria-hidden="true" />
              </motion.button>
              
              <motion.button 
                className="px-8 py-3 bg-transparent text-white border-2 border-white/80 rounded-lg font-medium hover:bg-white/10 transition-all focus:ring-4 focus:ring-white/30 focus:outline-none flex items-center group"
                whileHover={{ scale: 1.03, y: -2 }}
                whileTap={{ scale: 0.98 }}
                aria-label="Schedule a school tour"
              >
                <FaCalendarAlt className="mr-2" aria-hidden="true" />
                <span>Book a Tour</span>
              </motion.button>
            </motion.div>
          </div>
        </motion.div>
      </div>
    </div>
  );
}

export default Admissions;
